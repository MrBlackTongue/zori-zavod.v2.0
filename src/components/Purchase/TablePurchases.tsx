import React from 'react';
import {PurchaseTypes, TableProps} from '../../types';

export const TablePurchases: React.FC<TableProps<PurchaseTypes>> = ({ updateTable, openDrawer }) => {
    return <div>TablePurchases</div>
}